# Stable reference to a grasping-side workspace scene source.
#
# scene_name is the mobile-manipulator registry name.
# package_name and workspace_file identify the workspace.yaml file to load.
# scene_revision is an optional source revision or content hash.

string scene_name
string package_name
string workspace_file
string scene_revision