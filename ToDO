Motion planning follow-up

- Run `motion_execution_node` against hardware or MoveIt demo and verify nearby targets stay on the local joint branch.
- Temporarily set `log_joint_goal_deltas: true` in the active `motion_config.yaml` while testing nearby-IK behavior.
- Capture at least one previous failure case where the UR10 went the long way around and compare old vs new planning behavior.
- Tune `joint_state_timeout_sec`, `ik_timeout_sec`, and `joint_goal_tolerance_rad` based on runtime results.
- Decide whether `fallback_to_pose_planning_on_ik_failure` should remain enabled in production.
- If IK fallback happens often, add more logging around target pose, target frame, and current planning-joint state.
- Evaluate switching from KDL to TRAC-IK if nearby-IK still picks unstable branches.
- Add automated tests for joint-angle unwrapping and joint-goal request construction in `grasping_control`.
- Add a demo or test procedure to docs showing how to validate `/joint_states` freshness and `/compute_ik` availability.
